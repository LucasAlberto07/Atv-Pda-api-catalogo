# 📘 API Catálogo — CRUD em Memória com Node.js e Express

Este projeto foi desenvolvido como atividade prática para criar uma **API REST do zero**, utilizando Node.js, Express e uma arquitetura organizada em **Rotas**, **Controladores** e **Modelos (POO)**.  
A API implementa um CRUD completo utilizando um "banco de dados" em memória.

---
